import { Box, Typography } from '@mui/material';
import * as React from 'react';

const ShowUser= (props)=>{

  return (
    <Box> 
      <Typography variant="h1" component="h1" gutterBottom>To Be Implemented!!!</Typography>
    </Box>
  );
};
export default ShowUser;